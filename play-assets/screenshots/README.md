# Screenshots

## `real/` — REAL device captures (use these for the Play listing)

Captured from the connected phone (POCO, Android 15, `app.puregram`) on the final published
build **12.8.7 / 67619**, at 720×1600. Every "Telegram" branding leak is fixed and the
supervision views are shown **populated with real data** (a live demo supervision link was set
up, captured, then removed).

| File | Screen |
|------|--------|
| `01-chats-puregram-title.png` | Main chats list — action-bar title reads **"Puregram"** |
| `02-settings-puregram-section.png` | Settings ▸ **Puregram** section + "Puregram for Android v12.8.7" footer; Help rows now **FAQ / Features** |
| `03-link-supervisor-green-qr.png` | Link a supervisor — 60-second code + **green** Puregram QR + "Copy code" |
| `04-blocked-chat-toast.png` | Deny-by-default toast: "هذه المحادثة غير مسموح بها في Puregram" |
| `05-allowed-to-me.png` | "Allowed to me" — 4 allowed chats (green ✓) + "Parent supervises me" |
| `06-users-i-supervise.png` | "Users I supervise" — a supervised account ("Child Emulator · 7 chats · 2 allowed") |
| `07-approve-chats-toggles.png` | Per-chat approval — the supervisor toggles exactly which chats open |

720×1600 (9:16) PNGs — valid for Play (PNG/JPEG, 16:9 or 9:16, sides 320–3840, 2–8 images).
Pick any 2–8; a strong order is 01 → 05 → 07 → 03 → 04.

## `mockups/` — illustrative PIL mockups (optional)

Brand-accurate mockups generated before the phone was connected. The `real/` captures now cover
every screen (including the populated supervision views), so these are just an optional fallback.
