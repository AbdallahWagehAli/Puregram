"""Puregram standalone backend (FastAPI + PostgreSQL).

The control plane for puregram.app: a per-device Telegram whitelist API plus a
standalone, machine-bound license system and the admin panel that manages it.

See ../README.md for the full API contract and deploy runbook.
"""

__version__ = "1.0.0"
