<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <application android:name="org.telegram.messenger.ApplicationLoaderImpl" tools:replace="name">
        <service
            android:name="org.telegram.messenger.GoogleVoiceClientService"
            android:exported="true"
            android:foregroundServiceType="dataSync"
        />
        <activity
            android:name="org.telegram.messenger.GoogleVoiceClientActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.voicesearch.SEND_MESSAGE_TO_CONTACTS" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!--
             Puregram: removed the SMSJobsNotification service. It was the sticky
             notification for Telegram's SMS-relay programme, whose SEND_SMS and
             READ_CALL_LOG permissions are already gone, so the programme can never be
             joined and the service can never start. Its "remoteMessaging" foreground
             type was the only user of FOREGROUND_SERVICE_REMOTE_MESSAGING.
        -->
    </application>
</manifest>